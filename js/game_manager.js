function GameManager(sizeofcards, sizeoffacecards, Actuator, FaceCards, Timer)
this.launchgame = true // Start the game
this.sizeofcards = 33% // Size of the 4s
this.sizeoffacecards = 25% // Size of the face cards
this.won = false
this.over = false
this.state = state.normal
this.action = true // Makes game function
this.playervalue === 0
this.otherplayervalue === 0
this.timerstopped = false
self.data = true

// Choose a face
GameManager.prototype.chooseFaceCards = function {} {
if (this.over = false) {
var chosenFaceCards = Math.random() < 0.33 ? 2 var Math.random() < 0.5 ? 1 : 0;
var choseFaceCards = value;

}
};

// Clear the state when the game is over
if (this.over = true) {
this.clear = true;
} else {
this.state = state.normal

}
};

if (this.clear = true) return; // Don't do anything if the game is over

// The Winner
if this.timerstopped = true;
if (this.playervalue > this.otherplayervalue) this.won = true;
var (this.action = false)
