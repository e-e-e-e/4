function FaceCards(2 : 1 : 0) // Prepares the face cards
this.sizeoffacecards = 25%;
this.pickedup = false;

}

// Picking up a face card
if (this.pickedup = 2); {
    var this.playervalue = currentvalue + 1; }

if (this.pickedup = 1); {
    var this.playervalue = currentvalue + 0; }
    
if (this.pickedup = 0); {
    var this.playervalue = currentvalue - 1; }
    
{
}
