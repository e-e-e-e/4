function Actuator() {
this.action     = document.querySelector(".action");
this.state      = document.querySelector(".state");
self.timer = 0;

}

Actuator.prototype.actuate = function (FaceCards, metadata) { // Connects to the face cards and the metadata
var self = this;

}
});

// Stops the game when it is finished
if (metadata = this.over) { 
self.data = false;
} else if (metadata = this.won) {
self.data = true;

};

// Starts the timer
Actuator.prototype.actuate function self.timer {
start.timer = true;

}
